reload:
  name: Reload
  description: Reload HA Notifications configuration from notification_center.yaml.

test:
  name: Test alert
  description: Send a test notification for an HA Notifications alert.
  fields:
    alert_id:
      name: Alert ID
      description: The ID of the alert to test.
      required: true
      selector:
        text:
