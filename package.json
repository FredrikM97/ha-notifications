{
  "name": "ha-notifications-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && mkdir -p dist/brand custom_components/ha_notifications/dist/brand && esbuild frontend/panel.ts --bundle --format=esm --outfile=dist/panel.js && cp custom_components/ha_notifications/brand/icon.png dist/brand/icon.png && cp dist/panel.js custom_components/ha_notifications/dist/panel.js && cp dist/brand/icon.png custom_components/ha_notifications/dist/brand/icon.png",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "prettier --write frontend/**/*.ts tests/frontend_smoke.test.ts",
    "test:frontend": "vitest run tests/frontend_smoke.test.ts",
    "test:unit": "vitest run",
    "test:ui": "vitest run tests/frontend",
    "ui:preview": "npm run build && node scripts/ui_preview.mjs",
    "clean": "rm -rf dist",
    "export:hacs": "sh scripts/export_hacs.sh",
    "package:hacs": "sh scripts/package_hacs.sh",
    "install:ha": "sh scripts/install_local.sh"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.2",
    "@testing-library/user-event": "^14.6.7",
    "esbuild": "^0.28.2",
    "happy-dom": "^20.14.5",
    "lit": "^3.3.3",
    "prettier": "^3.9.9",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  },
  "dependencies": {
    "yaml": "^2.9.1"
  }
}
